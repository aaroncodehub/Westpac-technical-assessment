import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {
    flexGrow: 1,
    display: "flex",
    alignItems: "center",
  },
}));
export default useStyles;
